'use strict';

var compute_chain_median = require("./compute_chain_median");

function compute_median(collection) {
  //在这里写入代码
  var result = compute_chain_median(collection);
  return result;
}

module.exports = compute_median;


